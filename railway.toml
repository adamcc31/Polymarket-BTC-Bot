[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python scripts/execution_bot_v4.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[volumes]]
mountPath = "/app/logs"

[environments.production]
startCommand = "python scripts/execution_bot_v4.py"
